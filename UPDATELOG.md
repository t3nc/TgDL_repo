# 更新日志（Update Logs）

> 本文件记录 TDL 桌面客户端的每一次版本变更，**每次更新后必须同步更新本文档**。
> 新条目追加到文件顶部（最新在上），格式：`## [版本号] - 日期`。

## 版本规则

- 版本号遵循 `主版本.次版本.修订号`：
  - **修订号**：缺陷修复、不影响使用方式的内部改动
  - **次版本**：新增功能、界面/交互变更
  - **主版本**：不兼容的架构调整或配置格式变更
- 每个版本分三段描述：**新增 / 修复 / 变更**；没有内容的段落直接省略。
- 涉及构建方式、配置格式、内核版本的变更请在条目末尾加 **注意** 提示。
- **发布前必须在此处追加对应版本条目**：`npm run release` 会以该段落作为 GitHub Release 说明，
  缺少条目时发布脚本直接报错退出（版本号还需与 `package.json`、`src-tauri/tauri.conf.json` 一致）。
- 对外分发由 `npm run release` 完成，产物落在发布仓库
  [t3nc/TgDL_repo](https://github.com/t3nc/TgDL_repo)（详见 README「发布流程」）。

---

## [0.1.0] - 2026-09-16

首个可用版本。基于 Tauri 2 的 Windows 桌面客户端，内嵌 tdl 内核（预置 `0.20.4`），
提供批量下载、登录向导、任务队列与内核自更新能力。

### 新增

- **批量下载**
  - 每行一个 Telegram 消息链接，自动去重、合法性预校验，空行忽略
  - 导入官方客户端导出的 JSON 文件（`-f`）
  - 超长列表按命令行长度预算（20000 字符）自动拆分为多个子任务串行执行
  - 下载目录、文件名模板（变量/函数速查 + 实时预览）、扩展名白名单/黑名单（互斥）
  - 按 MIME 重写扩展名、跳过同名同大小、相册整组下载、反序下载、Takeout 会话
  - 断点续传策略：固定传入 `--continue` / `--restart`，避免交互式确认卡死
- **并发与节流**：稳健 / 均衡 / 极速三档预设，并支持并发任务数、单任务线程数、连接池、任务间隔微调
- **登录向导**：二维码扫码登录、手机号 + 验证码登录（含两步验证密码）、多账号命名空间隔离
- **任务队列**：独立页卡，按会话（DialogID）两级展开进度；状态筛选、取消（保留断点）、重试、打开目录、删除记录
- **重复下载检测**：本地下载历史；提交时提示已下载链接，可选「仍然全部下载」（`重下_` 前缀，不覆盖）或「跳过」
- **运行日志**：内置只读终端（xterm.js）原样呈现 tdl 的 ANSI 输出（二维码、彩色进度条）+ 结构化日志列表
- **内核管理**：显示版本与来源（内置 / 已更新 / 自定义路径）、检查更新、分步更新进度、多版本回滚、指定版本安装
- **设置中心**：代理、存储路径、NTP、重连超时、主题外观与配置维护，全部本地持久化
- **浏览器预览模式**：`npm run dev` 下所有后端调用回落到模拟实现，便于无 Rust 工具链时验收界面

### 修复

关键缺陷排查记录（完整分析见 README「已修复的关键缺陷」）：

1. 事件字段命名不匹配导致全部事件静默失效（`rename_all` 不作用于变体内部字段）
2. 终端订阅被误删导致二维码永不显示（改用 `clear()` 而非 `dispose()`）
3. 开发端口落在 Windows 保留区间（`EACCES`），改用 5173
4. 二维码因终端可视高度不足被截断
5. 验证码 / 两步验证步骤无法推进（交互提示不带换行符，新增「未结束行」通道）
6. 时长格式校验过严导致「开始下载」在默认配置下永久禁用（阻塞性）
7. 底部执行条跑到内容末尾（`position: fixed` 受 `transform` 祖先影响，改用 `sticky`）
8. 二维码模块缝隙导致手机无法识别（改为解析半块字符并用 canvas 重绘，附结构校验）
9. 下载完成后界面永远停在「下载中」（ConPTY 不给出 EOF，收尾顺序错误，阻塞性）
10. 进度条恒为 100%、数值显示成 `X/X`（标题与进度条同行导致解析错位）
11. 「生效参数快照」链接数恒为 0（开关型参数吞掉后续 `-u`）
12. 任务队列进度恒为 `0 B`（tdl 真实进度行无 `/总量`、进度条字符为 `#`、采样目录与 `-d` 不一致）
13. 任务已完成但子任务进度停在 92%（go-pretty 不渲染终帧，改为「事实通道」判定完成）

### 变更

- 项目结构定型：前端 `lib / hooks / store / components / pages`，后端 `commands / config / kernel / tdl / auth / download / history`
- 测试基线：前端 27 个单元测试通过；Rust 102 个单元测试 + `pty_exit` 集成测试通过（`pty_prompt` 默认忽略）

### 注意

- 可执行文件必须与同级 `resources/` 目录一起分发；单独拷贝 exe 会丢失内置内核，
  可在「内核管理」中点击「检查更新」重新获取。
- 构建期 `scripts/fetch-kernel.mjs` 为「尽力而为」，失败时会写入 `tdl.version = none` 但不中断构建。

---

## 模板（新增版本时复制）

```markdown
## [x.y.z] - YYYY-MM-DD

一句话概述。

### 新增

- ...

### 修复

- ...

### 变更

- ...
```
